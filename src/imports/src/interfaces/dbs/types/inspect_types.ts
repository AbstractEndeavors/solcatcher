/* ============================================================================
 * CALLER INFO HELPERS
 * ========================================================================== */

export interface CallerInfo {
  functionName?: string;
  methodName?: string;
  fileName?: string;
  lineNumber?: number;
  columnNumber?: number;
}
